# Models

> **Live lesson:** https://langchain-ai.github.io/lca-deepagents/m1/m1.3-models.html

*This lesson has two tabs: **Models** and **Lab** — shown below in order.*

---

## Models: Use Any Model Provider

> LangChain's model-agnostic design means you can swap in whatever fits your needs: a cheaper model to cut costs, an open-source model for privacy, or a more capable one for complex reasoning.

> **What is a model provider?** A model provider is a company or platform that hosts AI models and lets you call them through an API (e.g., OpenAI, Anthropic, Google). LangChain gives you one consistent way to call those models: install the provider package, pick a model name, and swap models without rewriting your agent code.
>
> LangChain supports 50+ chat model providers. See the full [chat model integrations](https://docs.langchain.com/oss/python/integrations/chat) list for what's available.

### Model Categories

| Category | Best for | Examples |
|---|---|---|
| Proprietary & Conversational | Fluid conversation, instruction-following, and general-purpose tasks | Claude Haiku/Sonnet, GPT-4.1, Gemini 2.0 Flash |
| Advanced Reasoning | Multi-step problem-solving, math, code, and logic-heavy tasks | Claude Opus 4, o3, Gemini 2.5 Pro |
| Open-Source & Open-Weight | Local or private deployment; lower cost and full control over the model | Llama 3.3, Mistral, Qwen |

The table above covers the main categories used in this course. For the full directory of 50+ supported chat model providers, see the [LangChain chat model integrations](https://docs.langchain.com/oss/python/integrations/chat).

Every lab gets its model from a single shared file, **`python/models.py`**. It exports a `model` object built with `init_chat_model(...)`, and the lab scripts simply do `from models import model`.

To use a different model or model provider, edit `models.py`:

```python
# python/models.py
model = init_chat_model("anthropic:claude-haiku-4-5")   # workshop default, fast & cheap
# model = init_chat_model("ollama:llama3.2")
# model = init_chat_model("openai:gpt-4.1-mini")
# model = init_chat_model("openai:gpt-4.1")
```

If you set up Anthropic or OpenAI in the setup lesson, those work out of the box. If you want to use Azure, Bedrock, or Google, those require an extra install (`uv sync --extra azure|bedrock|google`) and additional env vars; see the notes at the top of `models.py`.

Head to **Lab 1** to run this yourself and see how the output changes across models.

---

*Tab: Lab*

## Models (Lab)

First, run your scratch agent from the previous lesson with the default model:

```bash
cd ~/Documents/Github/lca-deepagents/python && uv run ./m1/m1.2_scratch_agent.py
```

Next, open `python/models.py` and switch the model: comment out the active Haiku line and uncomment another model line you want to try.

> 📹 [Video: model swap walkthrough — see live lesson]

After you change `models.py`, run the same script again and compare the output:

```bash
cd ~/Documents/Github/lca-deepagents/python && uv run ./m1/m1.2_scratch_agent.py
```

> **Try an open-source model via OpenRouter**
> OpenRouter gives you access to free hosted open-source models with no extra install. If you added your `OPENROUTER_API_KEY` to `.env` during setup, just uncomment the OpenRouter lines in `models.py` and run the lab again.
>
> Model used in this course: [nvidia/nemotron-3-ultra-550b-a55b:free on OpenRouter](https://openrouter.ai/nvidia/nemotron-3-ultra-550b-a55b:free)
>
> **Note:** Free models on hosted platforms typically mean you are consenting to the provider using your prompts and completions as training data. Avoid sending sensitive or proprietary information through free-tier endpoints.

Here are two real responses to "What is an LLM?", one from each model:

**claude-haiku-4-5** response:

An LLM (Large Language Model) is a type of artificial intelligence system trained on vast amounts of text data to predict and generate human language... *(expand in live lesson)*

**nvidia/nemotron-3-ultra-550b-a55b** response:

An LLM (Large Language Model) is a type of artificial intelligence model trained on vast amounts of text data... *(expand in live lesson)*

---

### Ollama: run a model locally

**[NO API KEY NEEDED]**

Ollama is a tool for downloading and running open-source models directly on your machine. No API key, no data sent to a third party.

> 📹 [Video: Ollama setup walkthrough — see live lesson]

**Step 1: Install Ollama** from [ollama.com](https://ollama.com)

**Step 2: Pull a model:**

```bash
ollama pull llama3.2
```

**Step 3: Update `python/models.py`.** Comment out the active line and uncomment the Ollama line:

```python
# model = init_chat_model("anthropic:claude-haiku-4-5")
model = init_chat_model("ollama:llama3.2")
```

**Step 4: Run the lab as normal.** No other code changes needed.

> **Local only.** Ollama also offers hosted cloud models at ollama.com, which do require an API key. These steps are for running a model locally on your own machine.

---

## Recap

- `init_chat_model("provider:model")` targets any LangChain-supported chat model with a single string; swap the prefix to switch model providers, swap the suffix to switch models
- All labs share one model object from `python/models.py`; changing that one line is the only code change needed to run a different model
- Three categories in this course: Proprietary & Conversational, Advanced Reasoning, and Open-Source/Open-Weight; 50+ chat model providers are supported in total
- Swapping the model changes nothing else in the agent; the system prompt, tools, and behavior instructions stay identical

---

## References

- [Deep Agents overview](https://docs.langchain.com/oss/python/deepagents/overview)
- [Models (Deep Agents)](https://docs.langchain.com/oss/python/deepagents/models)
- [Chat model integrations (LangChain)](https://docs.langchain.com/oss/python/integrations/chat)
