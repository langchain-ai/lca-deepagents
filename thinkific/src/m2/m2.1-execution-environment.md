[For translation, open lesson in new tab and use Chrome translate](https://langchain-ai.github.io/lca-deepagents/m2/m2.1-execution-environment.html)

<style>@import url('../shared/sd-components.css');</style>
<script src="../shared/sd-components.js"></script>
<script src="https://cdn.jsdelivr.net/npm/mermaid@11/dist/mermaid.min.js"></script>
<script>mermaid.initialize({ startOnLoad: true, theme: 'base', themeVariables: { background: '#F2FAFF', primaryColor: '#E5F4FF', primaryBorderColor: '#006DDD', primaryTextColor: '#030710', lineColor: '#2F4B68' } });</script>

# Execution Environment

<style>
.lt-bar {
  display: flex;
  flex-wrap: wrap;
  gap: 20px;
  margin: 28px 0 0;
  border-bottom: 2px solid #CCE9FF;
}
.lt-group { display: flex; gap: 3px; }
.lt-env  { --c: #006DDD; }
.lt-quiz { --c: #7C3AED; }
.lt-tab {
  font: 500 14px 'IBM Plex Mono', monospace;
  padding: 9px 14px;
  border: none;
  background: transparent;
  color: #40668D;
  cursor: pointer;
  border-bottom: 3px solid transparent;
  margin-bottom: -2px;
  border-radius: 6px 6px 0 0;
  transition: background .15s, color .15s, border-color .15s;
  white-space: nowrap;
}
.lt-tab:hover { background: #F2FAFF; color: #030710; }
.lt-tab.active {
  color: var(--c);
  border-bottom-color: var(--c);
  background: #fff;
}
.lt-panel { display: none; padding-top: 24px; }
.lt-panel.active { display: block; }
@media (max-width: 600px) {
  .lt-bar { flex-wrap: nowrap; overflow-x: auto; gap: 12px; }
  .lt-tab { padding: 8px 10px; font-size: 13px; }
}
</style>

<div class="lt-bar" role="tablist" aria-label="Lesson sections">
  <div class="lt-group lt-env">
    <button class="lt-tab active" data-p="env" role="tab" aria-selected="true">Execution Environment</button>
  </div>
  <div class="lt-group lt-quiz">
    <button class="lt-tab" data-p="quiz" role="tab" aria-selected="false">Quiz</button>
  </div>
</div>

<div class="lt-panel active" id="p-env" markdown="1" role="tabpanel">

<details class="video-walkthrough">
<summary>Video Walk-through [click to expand] — script available, video later</summary>
<br>

**Script (will be recorded):** A Deep Agent's execution environment gives it three capabilities: a filesystem to read and write files, a shell to run commands via the execute tool, and an interpreter to run code. The first two run through a swappable backend. The interpreter is the exception, which we'll come back to.

[Show the backend diagram]

Let's walk through this diagram. At the top, filesystem tools: read_file, write_file, ls, and so on, all flow down through the Backend. The backend is what you configure to control where files actually live and where commands actually run.

[Point to State]

The default is State. Files are kept in the agent's in-memory state. Nothing persists to disk, which is fine for simple use cases or demos.

[Point to Disk]

Filesystem backend persists files to local disk. Good for local development where you want files to actually exist on your machine.

[Point to Store]

Store connects to a remote storage backend, useful when your agent is deployed and needs persistent file access across runs.

[Point to Context Hub]

Context Hub is a managed backend that handles file storage in a hosted environment.

[Point to Sandbox and LocalShell, and the execute tool node]

We will cover Sandboxes in a later lesson.

[Point to Composite and the Router]

Composite is the most flexible option. It routes file operations to different backends depending on the path prefix, so you could have memory files going to one backend, working files to another, and so on. All within the same agent.

[Point to Custom]

Finally, Custom lets you implement your own backend if none of the built-ins fit your needs.

[Scroll to "The Interpreter"]

The interpreter is separate from all of this. Language models are unreliable at exact computation (such as calculations, data transformations and generating precise output) so rather than having the model try to reason through those, it writes code and the interpreter actually runs it, returning the real result. It runs as a tool in the agent's main loop, not through a backend. The last lesson of this module covers it in detail.

The key idea to take away from this lesson is that the backend is what makes the execution environment configurable. Swap it and you change where files live and where commands run, without changing anything else about the agent.

</details>

A Deep Agent has an **execution environment** that provides three things:

- a **filesystem** to read and write files
- a **shell** to run commands (the `execute` tool)
- an **interpreter** to run code

The first two run through a pluggable **backend** (below); the interpreter runs as a tool in the main loop.

---

## The backend landscape

<div class="mermaid">
graph TB
    Tools[Filesystem Tools] --> Backend[Backend]

    Backend --> State[State]
    Backend --> Disk[Filesystem]
    Backend --> Store[Store]
    Backend --> ContextHub[Context Hub]
    Backend --> Sandbox[Sandbox]
    Backend --> LocalShell[Local Shell]
    Backend --> Composite[Composite]
    Backend --> Custom[Custom]

    Composite --> Router{Routes}
    Router --> State
    Router --> Disk
    Router --> Store
    Router --> ContextHub

    Sandbox --> Execute["+ execute tool"]
    LocalShell --> Execute["+ execute tool"]

    classDef trigger fill:#CCE9FF,stroke:#006DDD,stroke-width:2px,color:#030710
    classDef hub fill:#7FC8FF,stroke:#006DDD,stroke-width:2px,color:#030710
    classDef process fill:#E5F4FF,stroke:#006DDD,stroke-width:1px,color:#030710
    classDef decision fill:#CCE9FF,stroke:#40668D,stroke-width:1.5px,color:#030710
    classDef output fill:#030710,stroke:#030710,stroke-width:1px,color:#F2FAFF

    class Tools trigger
    class Backend hub
    class State,Disk,Store,ContextHub,Sandbox,LocalShell,Composite,Custom process
    class Router decision
    class Execute output
</div>

Every filesystem and `execute` call passes through whichever backend you configure. The default keeps files in the agent's **state**; others persist them to local **disk** or a **store**, run them in an isolated **sandbox**, or send files to different backends by path prefix (a **composite**). Sandbox and local-shell backends also add the `execute` tool. The next two lessons cover each in detail.

---

## The interpreter

The interpreter lets the agent run code. Language models are unreliable at exact computation, so when a task needs a precise result - a calculation, a data transformation, a generated file - the agent writes code and the interpreter runs it, returning the actual output. It runs as a tool in the agent's main loop, alongside the agent's other tools. The final lesson of this module covers it in detail.

---

## Recap

- The execution environment gives the agent three capabilities: a **filesystem**, a **shell** (`execute`), and an **interpreter**
- The filesystem tools and `execute` all run through a swappable **backend**: swap it to change where files live and where commands run, without touching the agent
- The **interpreter** is the exception: it runs as a tool in the main loop, not through a backend

## Next

The next lesson covers the filesystem surface, followed by the backends behind it and, finally, the interpreter.

---

## References

**Documentation:**
- [Filesystem Backends](https://docs.langchain.com/oss/python/deepagents/backends)

</div>

<div class="lt-panel" id="p-quiz" role="tabpanel">

<h2>Check your understanding</h2>

<MCQ
    question="What three capabilities does a Deep Agent's execution environment provide?"
    choices='["Filesystem, database, and interpreter", "Shell, interpreter, and model", "Filesystem, shell (execute tool), and interpreter", "Filesystem, shell, and model"]'
    correctIndex={2}
    explanation="A Deep Agent's execution environment provides three things: a filesystem to read and write files, a shell to run commands via the execute tool, and an interpreter to run code."
/>

<MCQ
    question="What do filesystem tools and the execute tool both run through?"
    choices='["The agent main loop", "The LLM directly", "A swappable backend", "The interpreter"]'
    correctIndex={2}
    explanation="Every filesystem and execute call passes through whichever backend you configure. The backend is what makes the execution environment configurable — swap it and you change where files live and where commands run."
/>

<MCQ
    question="What is the default backend, and where does it keep files?"
    choices='["Filesystem backend; persists files to local disk", "State backend; keeps files in the agent in-memory state", "Store backend; writes to remote storage", "Context Hub; managed hosted storage"]'
    correctIndex={1}
    explanation="State is the default backend. Files live in the agent's in-memory state, scoped to the current thread. Nothing persists to disk, which is fine for simple use cases or demos."
/>

<MCQ
    question="How is the interpreter different from filesystem tools and the execute tool?"
    choices='["It runs through a separate Interpreter backend", "It is only available in the Sandbox backend", "It calls the LLM directly instead of executing code", "It runs as a tool in the agent main loop, not through a backend"]'
    correctIndex={3}
    explanation="The interpreter is the exception. Unlike filesystem tools and execute — which run through a backend — the interpreter runs as a tool in the agent's main loop."
/>

<MCQ
    question="Which backend routes file operations to different backends based on path prefix?"
    choices='["Custom", "Store", "Context Hub", "Composite"]'
    correctIndex={3}
    explanation="Composite routes different path prefixes to different backends. For example, /memories/ could go to Store while everything else falls back to State — all within the same agent."
/>

</div>

<script>
(function () {
  var tabs = document.querySelectorAll('.lt-tab');
  function show(p) {
    tabs.forEach(function (t) {
      var on = t.getAttribute('data-p') === p;
      t.classList.toggle('active', on);
      t.setAttribute('aria-selected', on ? 'true' : 'false');
    });
    document.querySelectorAll('.lt-panel').forEach(function (panel) {
      panel.classList.toggle('active', panel.id === 'p-' + p);
    });
  }
  tabs.forEach(function (t) {
    t.addEventListener('click', function () { show(t.getAttribute('data-p')); });
  });
})();
</script>
