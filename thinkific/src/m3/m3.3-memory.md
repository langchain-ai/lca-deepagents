[For translation, open lesson in new tab and use Chrome translate](https://langchain-ai.github.io/lca-deepagents/m3/m3.3-memory.html)

<style>@import url('../shared/sd-components.css');</style>
<script src="../shared/sd-components.js"></script>

# Memory

<style>
.lt-bar {
  display: flex;
  flex-wrap: wrap;
  gap: 20px;
  margin: 28px 0 0;
  border-bottom: 2px solid #CCE9FF;
}
.lt-group { display: flex; gap: 3px; }
.lt-mem  { --c: #0891B2; }
.lt-wrap { --c: #B45309; }
.lt-tab {
  font: 500 14px 'IBM Plex Mono', monospace;
  padding: 9px 14px;
  border: none;
  background: transparent;
  color: #40668D;
  cursor: pointer;
  border-bottom: 3px solid transparent;
  margin-bottom: -2px;
  border-radius: 6px 6px 0 0;
  transition: background .15s, color .15s, border-color .15s;
  white-space: nowrap;
}
.lt-tab:hover { background: #F2FAFF; color: #030710; }
.lt-tab.active {
  color: var(--c);
  border-bottom-color: var(--c);
  background: #fff;
}
.lt-panel { display: none; padding-top: 24px; }
.lt-panel.active { display: block; }
@media (max-width: 600px) {
  .lt-bar { flex-wrap: nowrap; overflow-x: auto; gap: 12px; }
  .lt-tab { padding: 8px 10px; font-size: 13px; }
}
</style>

<div class="lt-bar" role="tablist" aria-label="Lesson sections">
  <div class="lt-group lt-mem">
    <button class="lt-tab active" data-p="mem" role="tab" aria-selected="true">Memory</button>
  </div>
  <div class="lt-group lt-wrap">
    <button class="lt-tab" data-p="lab" role="tab" aria-selected="false">Lab</button>
  </div>
  <div class="lt-group lt-quiz">
    <button class="lt-tab" data-p="quiz" role="tab" aria-selected="false">Quiz</button>
  </div>
</div>

<div class="lt-panel active" id="p-mem" markdown="1" role="tabpanel">

Memory lets an agent carry durable knowledge across conversations: user preferences, project conventions, team workflows, recurring facts, and instructions the base model would not know on its own.

The key distinction is:

- **Checkpointers preserve thread history**: messages and state within a conversation thread.
- **Memory preserves durable knowledge**: file-backed facts and instructions that can be loaded into future runs, including different threads when the backend scope allows it.

<div style="text-align: center; margin: 1.5rem 0;">
<svg viewBox="0 0 920 440" width="760" role="img" aria-label="Diagram contrasting checkpointers and memory. Checkpointers: each thread (Thread A, Thread B, Thread C) has its own isolated message history that is not shared with other threads. Memory: a single durable file such as slash memories slash AGENTS dot md is read from and written to by any thread that matches its scope.">
  <rect width="920" height="440" fill="#0b0f1a" rx="12"/>
  <text x="460" y="32" text-anchor="middle" font-size="16" font-weight="500" fill="#E8EEF7" font-family="'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif">Checkpointer vs. memory</text>

  <!-- checkpointer panel -->
  <rect x="20" y="60" width="420" height="360" rx="10" fill="#11182B" stroke="#F59E0B" stroke-opacity="0.6" stroke-width="1.5"/>
  <text x="230" y="95" text-anchor="middle" font-size="16" font-weight="600" fill="#FBBF24" font-family="'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif">Checkpointer</text>
  <text x="230" y="118" text-anchor="middle" font-size="12" fill="#8FA6C2" font-family="'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif">scoped to a single thread</text>

  <rect x="50" y="145" width="360" height="55" rx="8" fill="#1B2338" stroke="#3A4560"/>
  <text x="70" y="168" font-size="13" font-weight="600" fill="#E8EEF7" font-family="'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif">Thread A</text>
  <text x="70" y="187" font-size="11" fill="#8FA6C2" font-family="'IBM Plex Mono', ui-monospace, monospace">messages in state</text>

  <rect x="50" y="215" width="360" height="55" rx="8" fill="#1B2338" stroke="#3A4560"/>
  <text x="70" y="238" font-size="13" font-weight="600" fill="#E8EEF7" font-family="'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif">Thread B</text>
  <text x="70" y="257" font-size="11" fill="#8FA6C2" font-family="'IBM Plex Mono', ui-monospace, monospace">messages in state</text>

  <rect x="50" y="285" width="360" height="55" rx="8" fill="#1B2338" stroke="#3A4560"/>
  <text x="70" y="308" font-size="13" font-weight="600" fill="#E8EEF7" font-family="'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif">Thread C</text>
  <text x="70" y="327" font-size="11" fill="#8FA6C2" font-family="'IBM Plex Mono', ui-monospace, monospace">messages in state</text>

  <text x="230" y="378" text-anchor="middle" font-size="11.5" font-style="italic" fill="#6B7A99" font-family="'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif">Each thread's history is isolated,</text>
  <text x="230" y="396" text-anchor="middle" font-size="11.5" font-style="italic" fill="#6B7A99" font-family="'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif">nothing here is shared with other threads.</text>

  <!-- memory panel -->
  <rect x="480" y="60" width="420" height="360" rx="10" fill="#11182B" stroke="#34D399" stroke-opacity="0.6" stroke-width="1.5"/>
  <text x="690" y="95" text-anchor="middle" font-size="16" font-weight="600" fill="#34D399" font-family="'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif">Memory</text>
  <text x="690" y="118" text-anchor="middle" font-size="12" fill="#8FA6C2" font-family="'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif">durable file, shared across threads</text>

  <rect x="590" y="145" width="200" height="50" rx="8" fill="#132018" stroke="#34D399" stroke-width="1.5"/>
  <text x="690" y="175" text-anchor="middle" font-size="12" fill="#A7F3D0" font-family="'IBM Plex Mono', ui-monospace, monospace">/memories/AGENTS.md</text>

  <defs>
    <marker id="mem-arrow" viewBox="0 0 10 10" refX="5" refY="5" markerWidth="6" markerHeight="6" orient="auto-start-reverse">
      <path d="M0,0L10,5L0,10z" fill="#34D399"/>
    </marker>
  </defs>
  <line x1="615" y1="206" x2="555" y2="240" stroke="#34D399" stroke-width="1.5" marker-start="url(#mem-arrow)" marker-end="url(#mem-arrow)"/>
  <line x1="690" y1="206" x2="690" y2="240" stroke="#34D399" stroke-width="1.5" marker-start="url(#mem-arrow)" marker-end="url(#mem-arrow)"/>
  <line x1="765" y1="206" x2="825" y2="240" stroke="#34D399" stroke-width="1.5" marker-start="url(#mem-arrow)" marker-end="url(#mem-arrow)"/>

  <rect x="500" y="250" width="110" height="50" rx="8" fill="#1B2338" stroke="#3A4560"/>
  <text x="555" y="272" text-anchor="middle" font-size="12" font-weight="600" fill="#E8EEF7" font-family="'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif">Thread A</text>
  <text x="555" y="288" text-anchor="middle" font-size="10" fill="#8FA6C2" font-family="'IBM Plex Mono', ui-monospace, monospace">reads · writes</text>

  <rect x="635" y="250" width="110" height="50" rx="8" fill="#1B2338" stroke="#3A4560"/>
  <text x="690" y="272" text-anchor="middle" font-size="12" font-weight="600" fill="#E8EEF7" font-family="'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif">Thread B</text>
  <text x="690" y="288" text-anchor="middle" font-size="10" fill="#8FA6C2" font-family="'IBM Plex Mono', ui-monospace, monospace">reads · writes</text>

  <rect x="770" y="250" width="110" height="50" rx="8" fill="#1B2338" stroke="#3A4560"/>
  <text x="825" y="272" text-anchor="middle" font-size="12" font-weight="600" fill="#E8EEF7" font-family="'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif">Thread C</text>
  <text x="825" y="288" text-anchor="middle" font-size="10" fill="#8FA6C2" font-family="'IBM Plex Mono', ui-monospace, monospace">reads · writes</text>

  <text x="690" y="378" text-anchor="middle" font-size="11.5" font-style="italic" fill="#6B7A99" font-family="'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif">One durable file, any thread with access</text>
  <text x="690" y="396" text-anchor="middle" font-size="11.5" font-style="italic" fill="#6B7A99" font-family="'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif">can read and write it.</text>
</svg>
</div>

---

## The mental model: memory is files

In Deep Agents, memory is plain files on the agent filesystem. The common convention is a markdown file such as `/memories/AGENTS.md`.

<div style="border-radius:10px;overflow:hidden;background:#0b0f1a;border:1px solid #1c2438;margin:1rem 0;">
<div style="display:flex;align-items:center;gap:8px;padding:10px 14px;background:#141b2e;border-bottom:1px solid #1c2438;">
<span style="width:11px;height:11px;border-radius:50%;background:#FF5F56;display:inline-block;"></span>
<span style="width:11px;height:11px;border-radius:50%;background:#FFBD2E;display:inline-block;"></span>
<span style="width:11px;height:11px;border-radius:50%;background:#27C93F;display:inline-block;"></span>
<span style="font-family:'IBM Plex Mono',ui-monospace,monospace;font-size:13px;color:#8FA6C2;margin-left:6px;">AGENTS.md</span>
</div>
<pre style="margin:0;padding:18px 20px;background:transparent;overflow-x:auto;"><code style="font-family:'IBM Plex Mono',ui-monospace,monospace;font-size:14px;line-height:1.6;color:#E6EDF3;white-space:pre;"><span style="color:#7FC8FF;">#</span> Project Guidelines

<span style="color:#7FC8FF;">##</span> Code Style
<span style="color:#8FA6C2;">-</span> Use type annotations
<span style="color:#8FA6C2;">-</span> Prefer pathlib.Path for file operations

<span style="color:#7FC8FF;">##</span> Workflow
<span style="color:#8FA6C2;">-</span> Run tests with: uv run pytest</code></pre>
</div>

The agent can update memory with the same filesystem tools it already knows, especially `edit_file`. There is no separate memory database API for the agent to learn.

The `memory` argument tells Deep Agents which files should be treated as memory:

```python {3}
agent = create_deep_agent(
    model=model,
    memory=["/memories/AGENTS.md"],
)
```

Without `memory=[...]`, the file may still exist in a backend, but it is not loaded into the system prompt as agent memory.

---

## How memory is injected

Passing `memory=[...]` creates `MemoryMiddleware` behind the scenes.

For each run, the middleware:

1. Loads the configured memory files from the backend
2. Stores the loaded contents in agent state
3. Appends the combined memory to the system prompt inside an `<agent_memory>` block on each model call in that run

That means memory is different from skills:

| | Memory | Skills |
|---|---|---|
| What is always visible? | Loaded memory content | Skill names and descriptions |
| When is full content loaded? | Proactively for the run | On demand when the agent activates a skill |
| Typical use | Durable facts and instructions | Reusable workflows and procedures |

Head to the **Lab** tab to see the `<agent_memory>` block in LangSmith.

---

## How memory is updated

The middleware also adds instructions telling the agent when to persist new information. When a user says "remember this" or provides context that should carry forward, the agent can call `edit_file` on one of the paths passed to `memory=[...]`.

That write updates the backend file. The new content is available after memory is reloaded in a later run.

A typical memory write looks like:

```text
User: Remember: the team switched to ruff for linting.
Agent: edit_file('/memories/AGENTS.md', ...)
```

Only store information that is safe and useful in future conversations. Do not store secrets such as API keys, credentials, tokens, or passwords.

---

## Where memory lives

The backend controls where memory files are stored. A common production pattern is to use a `CompositeBackend`: keep normal working files in the default backend, and route a dedicated `/memories/` directory to durable storage.

```python
from deepagents.backends import CompositeBackend, StateBackend, StoreBackend
from langgraph.store.memory import InMemoryStore

store = InMemoryStore()


def memory_namespace(runtime):
    user_id = runtime.context["user_id"]
    workspace_id = runtime.context["workspace_id"]
    return ("memory", workspace_id, user_id)


agent = create_deep_agent(
    model=model,
    backend=CompositeBackend(
        default=StateBackend(),
        routes={"/memories/": StoreBackend(namespace=memory_namespace)},
    ),
    store=store,
    memory=["/memories/AGENTS.md"],
)
```

In this setup, `/memories/AGENTS.md` is the logical path the agent sees. The `CompositeBackend` routes that path into `StoreBackend`, so memory persists beyond a single thread. Other working files can still use the default backend.

`StateBackend` is thread-scoped, so it is useful for temporary working files but not for durable long-term memory. `InMemoryStore()` is only for local development; in production, the store is usually backed by durable infrastructure.

| Component | Scope | Persists past the thread? | Use for |
|---|---|---|---|
| `StateBackend` | Single thread | No | Temporary working files |
| `StoreBackend` | Namespace you define (e.g. workspace/user) | Yes | Durable, long-term memory |
| `InMemoryStore()` | Process memory | No (lost on restart) | Local development only |
| `CompositeBackend` | N/A — routes paths to the backend above | Depends on the route | Splitting working files from memory files |

---

## Scoping memory

The `StoreBackend` namespace on the `/memories/` route determines which durable memory files the agent sees. In a real application, you usually derive that namespace from runtime context such as the authenticated user, workspace, tenant, or assistant ID.

Scope is not a fixed choice between "shared" and "per-user." It comes from which segments you include in the namespace tuple, and any combination is valid:

<details style="border:2.5px solid #000;border-radius:6px;background:#fff;margin:1rem 0;"><summary style="padding:10px 16px;cursor:pointer;font-weight:500;font-family:'IBM Plex Mono',monospace;font-size:14px;">Diagram: Venn diagram of memory scope combinations</summary><div style="padding:12px 16px 16px;">

<div style="text-align: center;">
<svg viewBox="0 0 900 1040" width="700" role="img" aria-label="Venn diagram of three overlapping circles for assistant_id, workspace_id, and user_id. Each region is a valid memory scope: assistant_id alone gives shared assistant memory; workspace_id alone gives workspace memory; user_id alone gives user memory that follows one person across threads; workspace_id plus user_id gives a user's private memory within one workspace; assistant_id plus user_id gives a user's private memory within one assistant. The assistant-plus-workspace overlap and the center where all three overlap are valid but rarely used." xmlns="http://www.w3.org/2000/svg" font-family="'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif">
  <rect width="900" height="1040" fill="#030710" rx="8"/>
  <text x="447" y="34" text-anchor="middle" font-size="17" font-weight="300" fill="#F2FAFF">Namespace segments compose your memory scope</text>
  <text x="447" y="58" text-anchor="middle" font-size="13" fill="#99D3FF" font-family="'IBM Plex Mono', ui-monospace, SFMono-Regular, Menlo, Consolas, monospace">mix any of assistant_id · workspace_id · user_id</text>

  <!-- three overlapping circles, pulled closer together so every combination region has real room -->
  <g style="isolation: isolate;">
    <circle cx="447" cy="456" r="300" fill="#7FC8FF" fill-opacity="0.4" stroke="#7FC8FF" stroke-width="2" style="mix-blend-mode: screen;"/>
    <circle cx="567" cy="664" r="300" fill="#34D399" fill-opacity="0.4" stroke="#34D399" stroke-width="2" style="mix-blend-mode: screen;"/>
    <circle cx="327" cy="664" r="300" fill="#FBBF24" fill-opacity="0.4" stroke="#FBBF24" stroke-width="2" style="mix-blend-mode: screen;"/>
  </g>

  <!-- assistant_id chip above its circle; user_id and workspace_id chips below, centered under their own circle instead of out at the edges -->
  <rect x="372" y="90" width="150" height="34" rx="6" fill="#161F34" stroke="#7FC8FF" stroke-width="1.5"/>
  <circle cx="389" cy="107" r="5" fill="#7FC8FF"/>
  <text x="403" y="111" font-size="12" fill="#CCE9FF" font-family="'IBM Plex Mono', ui-monospace, monospace">assistant_id</text>

  <rect x="252" y="985" width="150" height="34" rx="6" fill="#161F34" stroke="#FBBF24" stroke-width="1.5"/>
  <circle cx="269" cy="1002" r="5" fill="#FBBF24"/>
  <text x="283" y="1006" font-size="12" fill="#CCE9FF" font-family="'IBM Plex Mono', ui-monospace, monospace">user_id</text>

  <rect x="492" y="985" width="150" height="34" rx="6" fill="#161F34" stroke="#34D399" stroke-width="1.5"/>
  <circle cx="509" cy="1002" r="5" fill="#34D399"/>
  <text x="523" y="1006" font-size="12" fill="#CCE9FF" font-family="'IBM Plex Mono', ui-monospace, monospace">workspace_id</text>

  <!-- dark backing chips behind every label so text stays legible regardless of the blend color underneath -->
  <rect x="314" y="227" width="266" height="68" rx="10" fill="#03060D" fill-opacity="0.82"/>
  <rect x="599" y="727" width="274" height="68" rx="10" fill="#03060D" fill-opacity="0.82"/>
  <rect x="43" y="727" width="230" height="68" rx="10" fill="#03060D" fill-opacity="0.82"/>
  <rect x="128" y="470" width="310" height="60" rx="10" fill="#03060D" fill-opacity="0.82"/>
  <rect x="287" y="825" width="320" height="60" rx="10" fill="#03060D" fill-opacity="0.82"/>
  <rect x="496" y="480" width="230" height="40" rx="10" fill="#03060D" fill-opacity="0.82"/>
  <rect x="387" y="600" width="120" height="40" rx="10" fill="#03060D" fill-opacity="0.82"/>

  <!-- assistant only -->
  <text x="447" y="255" text-anchor="middle" font-size="15" fill="#EAF6FF" font-family="'IBM Plex Mono', ui-monospace, monospace">("memory", assistant_id)</text>
  <text x="447" y="281" text-anchor="middle" font-size="15" font-weight="500" fill="#FFFFFF">Shared assistant memory</text>

  <!-- workspace only -->
  <text x="736" y="755" text-anchor="middle" font-size="15" fill="#EAF6FF" font-family="'IBM Plex Mono', ui-monospace, monospace">("memory", workspace_id)</text>
  <text x="736" y="781" text-anchor="middle" font-size="15" font-weight="500" fill="#FFFFFF">Workspace memory</text>

  <!-- user only -->
  <text x="158" y="755" text-anchor="middle" font-size="15" fill="#EAF6FF" font-family="'IBM Plex Mono', ui-monospace, monospace">("memory", user_id)</text>
  <text x="158" y="781" text-anchor="middle" font-size="15" font-weight="500" fill="#FFFFFF">User memory</text>

  <!-- assistant + user -->
  <text x="283" y="496" text-anchor="middle" font-size="13" fill="#EAF6FF" font-family="'IBM Plex Mono', ui-monospace, monospace">("memory", assistant_id, user_id)</text>
  <text x="283" y="518" text-anchor="middle" font-size="14" font-weight="500" fill="#FFFFFF">Assistant + user</text>

  <!-- workspace + user -->
  <text x="447" y="851" text-anchor="middle" font-size="13" fill="#EAF6FF" font-family="'IBM Plex Mono', ui-monospace, monospace">("memory", workspace_id, user_id)</text>
  <text x="447" y="873" text-anchor="middle" font-size="14" font-weight="500" fill="#FFFFFF">User within workspace</text>

  <!-- assistant + workspace (rare) -->
  <text x="611" y="505" text-anchor="middle" font-size="14" font-weight="500" fill="#FFFFFF">assistant + workspace</text>

  <!-- all three (rare) -->
  <text x="447" y="625" text-anchor="middle" font-size="14" font-weight="500" fill="#FFFFFF">all three</text>
</svg>

<p><em>Every region is a valid namespace. The five combinations labeled in full match the scoping patterns below; assistant+workspace and all-three are valid but rarely needed.</em></p>

</div>

</div></details>

For example, a user-and-workspace scoped namespace might look like this:

```python
def memory_namespace(runtime):
    user_id = runtime.context["user_id"]
    workspace_id = runtime.context["workspace_id"]
    return ("memory", workspace_id, user_id)
```

Then invoke the agent with that context:

```python
agent.invoke(
    {"messages": [{"role": "user", "content": "Remember that I prefer concise answers."}]},
    context={"user_id": "u_123", "workspace_id": "acme"},
)
```

Common scoping patterns:

| Scope | Namespace shape | Use when... |
|---|---|---|
| Shared assistant memory | `("memory", assistant_id)` | Everyone should share the same durable instructions |
| User memory | `("memory", user_id)` | Preferences should follow one user across threads |
| Workspace memory | `("memory", workspace_id)` | A team/project should share conventions |
| User within workspace | `("memory", workspace_id, user_id)` | Users have private preferences inside a workspace |
| Assistant + user | `("memory", assistant_id, user_id)` | The same user may have different memories for different assistants |

Choose the scope deliberately. If private user memories share the same namespace, one user's preferences or context can leak into another user's agent run. If shared team memory is scoped too narrowly, the agent will fail to reuse conventions that should apply across the workspace.

---

## Recap

- Memory is not thread history; checkpointers preserve thread history
- Memory is durable knowledge stored as files
- `memory=[...]` activates `MemoryMiddleware` for those files
- Loaded memory is injected into the system prompt inside `<agent_memory>`
- Agents update memory by editing memory files
- The `/memories/` backend route determines persistence; the StoreBackend namespace determines scope

---

## References

**Documentation:**
- [Memory (Deep Agents)](https://docs.langchain.com/oss/python/deepagents/memory)
- [Filesystem Backends](https://docs.langchain.com/oss/python/deepagents/backends)
- [agents.md specification](https://agents.md/)

</div>

<div class="lt-panel" id="p-quiz" role="tabpanel">

<h2>Check your understanding</h2>

<MCQ
    question="What is the difference between checkpointers and memory?"
    choices='["Checkpointers preserve durable user preferences; memory preserves messages", "Checkpointers preserve thread history/state; memory stores durable file-backed knowledge", "They are two names for the same feature", "Memory is only for tool outputs; checkpointers are only for system prompts"]'
    correctIndex={1}
    explanation="Checkpointers preserve message history and state within a thread. Memory stores durable facts and instructions as files that can be loaded into future runs, including different threads when the backend scope allows it."
/>

<MCQ
    question="What does memory=[&quot;/memories/AGENTS.md&quot;] do?"
    choices='["It activates MemoryMiddleware for that file so its contents can be loaded and injected as memory", "It creates a new checkpointer for that path", "It makes every file under /memories read-only", "It converts the file into a skill"]'
    correctIndex={0}
    explanation="The memory argument tells Deep Agents which files should be treated as memory. Without it, a file may still exist in the backend, but it is not loaded into the system prompt as agent memory."
/>

<MCQ
    question="Where can you confirm memory injection in a LangSmith trace?"
    choices='["In the &lt;agent_memory&gt; block at the bottom of the system message", "Only in a separate Memory tab", "As the first ToolMessage in every run", "Only after the agent explicitly calls read_file"]'
    correctIndex={0}
    explanation="Loaded memory content is appended to the system prompt inside an &lt;agent_memory&gt; block on model calls in the run."
/>

<MCQ
    question="How does an agent persist new information to memory?"
    choices='["By returning a special memory_update key in its final response", "By calling edit_file on one of the configured memory file paths", "By creating a new skill", "By writing directly to the checkpointer"]'
    correctIndex={1}
    explanation="Memory is file-backed. When the agent should remember something, it edits a configured memory file such as /memories/AGENTS.md. That write persists to the backend and is available after memory is reloaded."
/>

<MCQ
    question="Why use StoreBackend for memory?"
    choices='["To make memory files available through durable backend storage and namespaces", "To disable edit_file", "To load skills only on demand", "To replace the system prompt"]'
    correctIndex={0}
    explanation="StoreBackend stores files in a key/value store under a namespace. In the common CompositeBackend pattern, routing `/memories/` to StoreBackend makes that directory durable and scopeable by user, workspace, tenant, or assistant."
/>

<MCQ
    question="What is the main risk of choosing the wrong memory namespace?"
    choices='["The model will stop seeing tool schemas", "Private memory can be shared with the wrong user or workspace", "The file extension changes from .md to .json", "The agent can no longer use skills"]'
    correctIndex={1}
    explanation="The namespace controls which files the agent sees. If private memories share a namespace, one user's preferences or context can leak into another user's run."
/>

</div>

<div class="lt-panel" id="p-lab" markdown="1" role="tabpanel">

## Lab: Coding Assistant with Memory

Open `python/m3/m3.3_memory_agent.py` to see how memory is wired through `CompositeBackend`: normal working files use the default backend, while `/memories/` routes to `StoreBackend`. The script derives the memory namespace from runtime context, seeds `/memories/AGENTS.md` into an in-memory store, then runs the agent twice. The first invoke answers a question using the memory content; the second triggers a memory write.

<Tip>

<details style="border:2.5px solid #000;border-radius:6px;background:#fff;margin:1rem 0;"><summary style="padding:10px 16px;cursor:pointer;font-weight:500;font-family:'IBM Plex Mono',monospace;font-size:14px;">Lab Video Walkthrough</summary><div style="padding:12px 16px 16px;"><div class="video-container" style="max-width:750px;"><div class="video-wrapper"><iframe src="https://share.descript.com/embed/0rmWKU64h0G" frameborder="0" allow="autoplay; fullscreen; encrypted-media; picture-in-picture" allowfullscreen></iframe></div></div></div></details>

**[View this run here in LangSmith.](https://smith.langchain.com/public/75a62f82-3517-4f3b-a447-98b70c346560/r)** Notice the `<agent_memory>` block in the system prompt holding the loaded `/memories/AGENTS.md` content, and the agent answering directly from it.

**[View this run here in LangSmith.](https://smith.langchain.com/public/80dc2c14-f909-41cb-94c8-eefa3d99b7ad/r)** Notice the agent calling `edit_file` on `/memories/AGENTS.md` to add the note about switching to ruff for linting.

<details style="border:2.5px solid #000;border-radius:6px;background:#E5F4FF;margin:1rem 0;"><summary style="padding:10px 16px;cursor:pointer;font-weight:500;font-family:'IBM Plex Mono',monospace;font-size:14px;">LangSmith Walkthrough</summary><div style="padding:12px 16px 16px;"><div class="video-container" style="max-width:750px;"><div class="video-wrapper"><iframe src="https://share.descript.com/embed/cJ0X5fXAUah" frameborder="0" allow="autoplay; fullscreen; encrypted-media; picture-in-picture" allowfullscreen></iframe></div></div></div></details>

</Tip>

---

### Run it

Before running it, make sure your model API key from setup is available in the Python environment.

From the repo root:

```bash
cd python
uv run ./m3/m3.3_memory_agent.py
```

After both invokes complete, the script prints the stored `/memories/AGENTS.md` content from the store. Verify that the ruff note was written into that stored memory file.

</div>

<script>
(function () {
  var tabs = document.querySelectorAll('.lt-tab');
  function show(p) {
    tabs.forEach(function (t) {
      var on = t.getAttribute('data-p') === p;
      t.classList.toggle('active', on);
      t.setAttribute('aria-selected', on ? 'true' : 'false');
    });
    document.querySelectorAll('.lt-panel').forEach(function (panel) {
      panel.classList.toggle('active', panel.id === 'p-' + p);
    });
  }
  tabs.forEach(function (t) {
    t.addEventListener('click', function () { show(t.getAttribute('data-p')); });
  });
})();
</script>
