[🔗 For translation, open lesson in new tab and use Chrome translate](https://langchain-ai.github.io/lca-deepagents/m1/m1.l4-system-prompt-quiz.html)

<style>@import url('../shared/sd-components.css');</style>
<script src="../shared/sd-components.js"></script>

# Quiz: The System Prompt

<MCQ
    question="How many named parts does create_deep_agent assemble the system prompt from?"
    choices='["One — the BASE prompt only", "Two — USER and BASE", "Four — USER, BASE, SUFFIX, and a tool section", "Three — USER, BASE, and SUFFIX"]'
    correctIndex={3}
    explanation="The assembled prompt has three parts: USER (your system_prompt argument), BASE (the SDK's built-in BASE_AGENT_PROMPT), and SUFFIX (appended by a HarnessProfile for model-specific tuning). Only USER and BASE are directly relevant to this lesson; SUFFIX is optional and covered separately."
/>

<MCQ
    question="Where in the assembled prompt does the USER segment appear?"
    choices='["At the very front, before BASE and SUFFIX", "At the very end, after the SUFFIX", "After the BASE instructions", "Interleaved with the BASE instructions"]'
    correctIndex={0}
    explanation="The SDK slots the USER segment — your system_prompt — at the very front of the assembled prompt, ahead of the BASE instructions and any SUFFIX."
/>

<MCQ
    question="You write SYSTEM_PROMPT = 'You are a pirate.' but the agent replies with no pirate tone. What is most likely wrong?"
    choices='["The BASE prompt overrides all user-defined personas", "The model requires a special flag to enable custom personas", "SYSTEM_PROMPT was defined but not passed to create_deep_agent", "The SUFFIX always overrides the USER segment"]'
    correctIndex={2}
    explanation="Defining the string is not enough. It must be passed explicitly: create_deep_agent(model=model, system_prompt=SYSTEM_PROMPT). Without the argument, the agent never receives the USER segment and falls back to BASE behavior only."
/>

<MCQ
    question="What is the BASE segment of the assembled prompt?"
    choices='["The system_prompt string you pass to create_deep_agent", "The tool schemas appended by the SDK before each LLM call", "A model-specific tuning block provided by a HarnessProfile", "The SDK's built-in BASE_AGENT_PROMPT — behavior guidance always present"]'
    correctIndex={3}
    explanation="The BASE segment is BASE_AGENT_PROMPT, built into the SDK. It carries the agent's core behavior guidance and is always present regardless of what you pass. The SUFFIX — from a HarnessProfile — is the model-specific part."
/>

<MCQ
    question="You switch from a pirate persona to a cowboy persona by changing SYSTEM_PROMPT. What else in the assembled prompt changes?"
    choices='["The BASE instructions adapt to match the new persona", "Nothing — only your USER segment changes", "The SUFFIX is regenerated based on the persona", "The tool schemas update to reflect the new persona's capabilities"]'
    correctIndex={1}
    explanation="Only your USER segment changes. The BASE instructions, SUFFIX, and tool schemas are identical across runs. The USER segment is the single lever you control; everything else stays put."
/>
